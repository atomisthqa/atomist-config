{
  "configuration": {
    "push": {
      "configuration": {
        "emoji-style": "default",
        "fingerprints": {
          "about-hint": "true",
          "render-unchanged": "true",
          "style": "fingerprint-inline",
          "data": "${file://fingerprints.json}"
        },
        "build": {
          "style": "decorator"
        },
        "generic": {
          "functions": [
              "${file://cf-buttons.js}",
              "${file://cf-push-button.js}"
          ]
        }
      }
    }
  }
}
